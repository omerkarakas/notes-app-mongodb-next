import NoteForm from "@/components/NoteForm";
import React from "react";

type Props = {};

const AddNotePage = (props: Props) => {
  return <NoteForm action="Add" />;
};

export default AddNotePage;
